[package]
name = "termo-rs"
version = "0.1.0"
edition = "2024"
rust-version = "1.98"
publish = false

[lib]
path = "lib.rs"
crate-type = ["rlib"]

[profile.release]
panic = "abort"
overflow-checks = true
lto = "fat"
codegen-units = 1
